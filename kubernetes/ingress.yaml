apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pokelike-ingress
  annotations:
    kubernetes.io/ingress.allow-http: "false"
    networking.gke.io/managed-certificates: "pokelike-certificate"
spec:
  ingressClassName: "gce"
  defaultBackend:
    service:
      name: pokelike-api
      port:
        number: 443
